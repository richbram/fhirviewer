export interface CCDASection {
    title: string;
    code: string;
    displayName?: string;
    text?: string;
}

export interface CCDANavSection {
    title: string;
    resourceType: string;
    index: number;
}
